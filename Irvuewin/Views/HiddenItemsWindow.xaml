<helpers:LocationAwareWindow
    x:Class="Irvuewin.Views.HiddenItemsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="clr-namespace:Irvuewin.Helpers"
    xmlns:viewModels="clr-namespace:Irvuewin.ViewModels"
    xmlns:converters="clr-namespace:Irvuewin.Helpers.Converters"
    mc:Ignorable="d"
    Title="{Binding Source={x:Static helpers:Localization.Instance}, Path=[HiddenItems_WindowTitle]}"
    Height="440" Width="520" MinWidth="400" MaxWidth="520"
    Background="{DynamicResource WindowBackgroundBrush}"
    ShowInTaskbar="False" ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">

    <helpers:LocationAwareWindow.DataContext>
        <viewModels:HiddenItemsViewModel />
    </helpers:LocationAwareWindow.DataContext>

    <helpers:LocationAwareWindow.Resources>
        <ResourceDictionary>
            <converters:ImageUrlToBitmapConverter x:Key="ImageUrlToBitmapConverter" />

            <!-- Tab style matching Settings window -->
            <Style x:Key="HiddenItemsTabItemStyle" TargetType="TabItem" BasedOn="{StaticResource TabStyle}">
                <Setter Property="Width" Value="100" />
            </Style>

            <!-- Center TabControl Style (consistent with Settings) -->
            <Style x:Key="HiddenItemsTabControlStyle" TargetType="TabControl">
                <Setter Property="Margin" Value="20,10,20,20" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabControl">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TabPanel x:Name="HeaderPanel"
                                          Grid.Row="0"
                                          Panel.ZIndex="1"
                                          IsItemsHost="True"
                                          HorizontalAlignment="Center"
                                          Background="Transparent" />
                                <Border x:Name="ContentPanel"
                                        Grid.Row="1"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}">
                                    <ContentPresenter x:Name="PART_SelectedContentHost"
                                                      ContentSource="SelectedContent"
                                                      Margin="{TemplateBinding Padding}"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </helpers:LocationAwareWindow.Resources>

    <Grid>
        <TabControl Style="{StaticResource HiddenItemsTabControlStyle}">

            <!-- Tab 1: Hidden Authors -->
            <TabItem Header="{Binding Source={x:Static helpers:Localization.Instance}, Path=[HiddenItems_Tab_Authors]}"
                     helpers:TabItemHelper.IconGeometry="{StaticResource Icon_Author}"
                     Style="{StaticResource HiddenItemsTabItemStyle}">
                <Grid>
                    <!-- Author List -->
                    <ListBox ItemsSource="{Binding HiddenAuthors}"
                             Margin="0,10"
                             BorderThickness="1"
                             BorderBrush="{DynamicResource BorderBrush}"
                             Background="{DynamicResource WindowBackgroundBrush}">
                        <ListBox.Style>
                            <Style TargetType="ListBox">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasHiddenAuthors}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.Style>
                        <ListBox.Template>
                            <ControlTemplate TargetType="ListBox">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5">
                                    <ScrollViewer x:Name="ListBoxScrollViewer"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </ListBox.Template>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding}" VerticalAlignment="Center" Margin="5,0" />
                                    <Button Grid.Column="1" Content="✕" Width="16" Height="16" Margin="0"
                                            Command="{Binding DataContext.RemoveHiddenAuthorCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ButtonStyle}"
                                            Foreground="{DynamicResource DangerBrush}"
                                            ToolTip="{Binding Source={x:Static helpers:Localization.Instance}, Path=[HiddenItems_UnhideTooltip]}" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="5,5" />
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}"
                                                    CornerRadius="5"
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter x:Name="ContentPresenter"
                                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background"
                                                            Value="{DynamicResource AccentBrush}" />
                                                    <Setter Property="TextElement.Foreground"
                                                            Value="{DynamicResource AccentForegroundBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background"
                                                            Value="{DynamicResource ControlMouseOverBrush}" />
                                                    <Setter Property="TextElement.Foreground"
                                                            Value="{DynamicResource PrimaryTextBrush}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <!-- Empty State -->
                    <TextBlock Text="{Binding Source={x:Static helpers:Localization.Instance}, Path=[HiddenItems_NoAuthors]}"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasHiddenAuthors}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </TabItem>

            <!-- Tab 2: Hidden Photos -->
            <TabItem Header="{Binding Source={x:Static helpers:Localization.Instance}, Path=[HiddenItems_Tab_Photos]}"
                     helpers:TabItemHelper.IconGeometry="{StaticResource Icon_Photo}"
                     Style="{StaticResource HiddenItemsTabItemStyle}">
                <Grid>
                    <!-- Photo Grid -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Margin="0,10">
                        <ScrollViewer.Style>
                            <Style TargetType="ScrollViewer">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasHiddenPhotos}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ScrollViewer.Style>
                        <ItemsControl ItemsSource="{Binding HiddenPhotos}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5" Width="140" Height="100">
                                        <!-- Photo thumbnail -->
                                        <Image Source="{Binding Urls.Small, Converter={StaticResource ImageUrlToBitmapConverter}, ConverterParameter=280}">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Setter Property="Stretch" Value="UniformToFill" />
                                                    <Setter Property="Clip">
                                                        <Setter.Value>
                                                            <RectangleGeometry RadiusX="5" RadiusY="5" Rect="0,0,140,100" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                        <!-- Unhide button overlay -->
                                        <Button Width="20" Height="20"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Top"
                                                Margin="0,4,4,0"
                                                Content="✕"
                                                FontSize="10"
                                                Command="{Binding DataContext.RemoveHiddenPhotoCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="{Binding Source={x:Static helpers:Localization.Instance}, Path=[HiddenItems_UnhideTooltip]}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="#99000000" />
                                                    <Setter Property="Foreground" Value="White" />
                                                    <Setter Property="BorderThickness" Value="0" />
                                                    <Setter Property="Cursor" Value="Hand" />
                                                    <Setter Property="Opacity" Value="0.7" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="Bd"
                                                                        Background="{TemplateBinding Background}"
                                                                        CornerRadius="10"
                                                                        Padding="2">
                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                      VerticalAlignment="Center" />
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="Bd" Property="Background"
                                                                                Value="{DynamicResource DangerBrush}" />
                                                                        <Setter Property="Opacity" Value="1.0" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Empty State -->
                    <TextBlock Text="{Binding Source={x:Static helpers:Localization.Instance}, Path=[HiddenItems_NoPhotos]}"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasHiddenPhotos}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</helpers:LocationAwareWindow>

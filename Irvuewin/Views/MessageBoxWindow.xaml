<Window x:Class="Irvuewin.Views.MessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Notification"
        Height="Auto" Width="Auto"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="5" Margin="20" MinWidth="250" MaxWidth="350">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Direction="-90" RenderingBias="Quality" ShadowDepth="5" Opacity="0.15"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title -->
                <RowDefinition Height="Auto"/> <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock x:Name="TitleText" Grid.Row="0"
                       Text="Title"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="#333333" Margin="0,0,0,15"/>

            <!-- Content Area -->
            <Grid Grid.Row="1" Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <Path x:Name="IconPath" Grid.Column="0" 
                      Width="32" Height="32" 
                      Margin="0,0,20,0"
                      Stretch="Uniform" 
                      VerticalAlignment="Top"
                      Data="M12,2A10,10 0 1,0 22,12A10,10 0 0,0 12,2M11,17H13V11H11V17M12,9A1,1 0 1,0 11,8A1,1 0 0,0 12,9Z"
                      Fill="#3585FF"/>

                <!-- Message -->
                <TextBlock x:Name="MessageText" Grid.Column="1"
                           Text="This is a message content."
                           FontSize="14" Foreground="#555555"
                           TextWrapping="Wrap" 
                           LineHeight="22"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- Buttons -->
            <StackPanel x:Name="ButtonPanel" Grid.Row="2" 
                        Orientation="Horizontal" HorizontalAlignment="Right">
                
                <Button x:Name="BtnYes" Content="Yes" 
                        Click="Btn_Click" Visibility="Collapsed"
                        Background="#3585FF" Foreground="White" BorderBrush="#3585FF"/>
                
                <Button x:Name="BtnNo" Content="No" 
                        Click="Btn_Click" Visibility="Collapsed"/>
                
                <Button x:Name="BtnOK" Content="OK" 
                        Click="Btn_Click" Visibility="Collapsed"
                        Background="#3585FF" Foreground="White" BorderBrush="#3585FF"/>
                
                <Button x:Name="BtnCancel" Content="Cancel" 
                        Click="Btn_Click" Visibility="Collapsed"/>
                
            </StackPanel>
        </Grid>
    </Border>
</Window>

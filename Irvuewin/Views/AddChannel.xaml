<helper:LocationAwareWindow
    x:Class="Irvuewin.Views.AddChannel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:Irvuewin.Helpers"
    xmlns:vm="clr-namespace:Irvuewin.ViewModels"
    xmlns:m="clr-namespace:Irvuewin.Models.Unsplash"
    xmlns:converters="clr-namespace:Irvuewin.Helpers.Converters"
    mc:Ignorable="d"
    Title="AddChannel" Icon="/Icons/application_64.ico"
    Height="450" Width="400"
    Background="WhiteSmoke"
    ShowInTaskbar="False" ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">

    <helper:LocationAwareWindow.DataContext>
        <vm:AddChannelViewModel />
    </helper:LocationAwareWindow.DataContext>

    <helper:LocationAwareWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:TextToVisibilityConverter x:Key="TextToVisibilityConverter" />

            <DataTemplate x:Key="ResolvingChannelsTemplate"
                          DataType="{x:Type m:UnsplashChannel}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <RadioButton x:Name="PART_RadioButton"
                                 Grid.Column="0"
                                 IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected, Mode=TwoWay}"
                                 IsEnabled="{TemplateBinding IsEnabled}"
                                 Margin="5,0,0,0"
                                 HorizontalAlignment="Right"
                                 Style="{StaticResource RadioStyle}" />
                    <Image Grid.Column="1"
                           Source="{Binding  Path= CoverPhoto.Urls.Small}">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Stretch" Value="UniformToFill" />
                                <Setter Property="Clip">
                                    <Setter.Value>
                                        <RectangleGeometry RadiusX="5" RadiusY="5" Rect="5,0,40,40" />
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Image.Style>
                    </Image>
                    <TextBlock Grid.Column="2"
                               Text="{Binding Title}"
                               Style="{StaticResource TextBlockStyle}"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap">
                        <TextBlock.ToolTip>
                            <ToolTip Style="{StaticResource ToolTipStyle}"
                                     Content="{Binding Title}" />
                        </TextBlock.ToolTip>
                    </TextBlock>

                    <Image Grid.Column="3"
                           Source="/Icons/channels/eye.ico"
                           Width="14" Height="14" Margin="5,0">
                    </Image>
                    <TextBlock Grid.Column="4"
                               Padding="0,0,20,0"
                               Text="{Binding TotalPhotos}"
                               Style="{StaticResource TextBlockStyle}"
                               HorizontalAlignment="Right">
                    </TextBlock>
                </Grid>
            </DataTemplate>

            <Style x:Key="ListBoxStyle2" TargetType="ListBox" BasedOn="{StaticResource ListBoxStyle}">
                <Setter Property="Margin" Value="0,5, 0,0" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5">
                                <ScrollViewer x:Name="ListBoxScrollViewer"
                                              HorizontalScrollBarVisibility="Disabled"
                                              VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- TODO: 可以抽取为公共样式 -->
            <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
                <!-- 基础属性设置 -->
                <Setter Property="Background" Value="WhiteSmoke" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Margin" Value="0,2" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter x:Name="ContentPresenter"
                                                  HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalAlignment}" />
                            </Border>
                            <!-- 视觉状态管理 -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="LightGray" />
                                    <Setter TargetName="ContentPresenter" Property="TextElement.Foreground"
                                            Value="Black" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="LightGray" />
                                    <Setter TargetName="ContentPresenter" Property="TextElement.Foreground"
                                            Value="Black" />
                                    <!-- <Setter TargetName="PART_RadioButton" Property="IsChecked" Value="True" /> -->
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ContentPresenter" Property="TextElement.Foreground"
                                            Value="#FF888888" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>


        </ResourceDictionary>
    </helper:LocationAwareWindow.Resources>


    <TabControl Margin="20, 0" BorderThickness="0" Background="WhiteSmoke">

        <TabItem>
            <TabItem.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Background="WhiteSmoke"
                                        BorderThickness="0"
                                        BorderBrush="Gray"
                                        Padding="3"
                                        CornerRadius="2">
                                    <ContentPresenter ContentSource="Header" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabItem.Resources>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.Resources>
                        <Style x:Key="PlaceholderTextBoxStyle" TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Grid>
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="2" />
                                            <ScrollViewer x:Name="PART_ContentHost"
                                                          Focusable="False"
                                                          HorizontalScrollBarVisibility="Hidden"
                                                          VerticalScrollBarVisibility="Hidden" />
                                            <TextBlock x:Name="PlaceholderText"
                                                       Text="{TemplateBinding Tag}"
                                                       Foreground="LightGray"
                                                       IsHitTestVisible="False"
                                                       Margin="{TemplateBinding Padding}"
                                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                       HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="FontStyle" Value="Italic" />
                                                        <Style.Triggers>
                                                            <!-- 当 TextBox 有文本时隐藏占位符 -->
                                                            <DataTrigger
                                                                Binding="{Binding Text , RelativeSource={RelativeSource AncestorType=TextBox}, Converter={StaticResource TextToVisibilityConverter}}"
                                                                Value="Collapsed">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                            <!-- 当 TextBox 获得焦点时隐藏占位符 -->
                                                            <DataTrigger
                                                                Binding="{Binding IsFocused, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                                Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style x:Key="ButtonStyles" TargetType="Button" BasedOn="{StaticResource BottomButtonStyle}">
                            <Setter Property="Height" Value="24" />
                            <Setter Property="Width" Value="24" />
                        </Style>
                    </Grid.Resources>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="40" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 x:Name="InputChannelUrl"
                                 Style="{StaticResource PlaceholderTextBoxStyle}"
                                 Tag="{Binding InputBox}"
                                 Text=""
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Padding="5" />
                        <Button Grid.Column="1"
                                Style="{StaticResource ButtonStyles}"
                                Click="ResolvingChannel_Click"
                                Content="{Binding Resolving}" />
                    </Grid>
                </Grid>

                <StackPanel Grid.Row="1">
                    <StackPanel.Resources>
                        <Style x:Key="HintTextStyle" TargetType="TextBlock">
                            <Setter Property="FontStyle" Value="Italic" />
                            <Setter Property="Foreground" Value="Gray" />
                            <Setter Property="FontSize" Value="10" />
                            <Setter Property="Padding" Value="5,0,0,5" />
                        </Style>
                    </StackPanel.Resources>
                    <TextBlock Text="{Binding HintTitle}"
                               Style="{StaticResource HintTextStyle}" />
                    <TextBlock Text="{Binding Hint1}"
                               Style="{StaticResource HintTextStyle}" />
                    <TextBlock Text="{Binding Hint2}"
                               Style="{StaticResource HintTextStyle}" />
                </StackPanel>

                <Separator Grid.Row="2" Style="{StaticResource SeparatorStyle}" />


                <ListBox Grid.Row="3" x:Name="PreChannelsListBox"
                         SelectionMode="Multiple"
                         helper:ListBoxSelectedItemsBehavior.BindableSelectedItems="{Binding SelectedChannels, Mode=TwoWay}"
                         Style="{StaticResource ListBoxStyle2}"
                         ItemsSource="{Binding PreChannels}"
                         ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                         ItemTemplate="{StaticResource ResolvingChannelsTemplate}">

                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>

                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="取消"
                            Style="{StaticResource BottomButtonStyle}"
                            Click="CancelAddChannel_Click" />
                    <Button Content="添加"
                            Style="{StaticResource BottomButtonStyle}"
                            Click="AddChannel_Click" />

                </StackPanel>
            </Grid>
        </TabItem>

    </TabControl>
</helper:LocationAwareWindow>
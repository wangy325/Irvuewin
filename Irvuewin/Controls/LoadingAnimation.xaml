<UserControl x:Class="Irvuewin.Controls.LoadingAnimation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Irvuewin.Controls"
             xmlns:converters="clr-namespace:Irvuewin.Helpers.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <!-- circle -->
    <Canvas>
        <Path x:Name="LoadingRing"
              Stroke="LightGray"
              StrokeThickness="3"
              StrokeDashArray="15,5"
              Data="M 8,8 m -8,0 a 8,8 0 1,1 16,0 a 8,8 0 1,1 -16,0"
              Visibility="{Binding IsLoading,
              RelativeSource={RelativeSource AncestorType=UserControl}, 
              Converter={StaticResource BooleanToVisibilityConverter}}">
            <Path.RenderTransform>
                <RotateTransform x:Name="RingRotation" Angle="0" CenterX="8" CenterY="8"/>
            </Path.RenderTransform>
            <Path.Triggers>
                <EventTrigger RoutedEvent="Path.Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation 
                                Storyboard.TargetName="RingRotation" 
                                Storyboard.TargetProperty="Angle"
                                From="0" To="360" Duration="0:0:2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Path.Triggers>
        </Path>
    </Canvas>
    
    <!-- ellipse -->
    <!--<Canvas>
        <Ellipse x:Name="LoadingCircle"
                 Visibility="{Binding IsLoading, 
                 RelativeSource={RelativeSource AncestorType=UserControl}, 
                 Converter={StaticResource BooleanToVisibilityConverter}}"
                 Width="20"
                 Height="20"
                 Canvas.Left="10"
                 Canvas.Top="15"
                 Fill="Gray">
            <Ellipse.OpacityMask>
                <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.5,0.5" RadiusX="0.5"
                                     RadiusY="0.5">
                    <GradientStop x:Name="GradientStop1" Offset="0" Color="#FF7F7F7F" />
                    <GradientStop x:Name="GradientStop2" Offset="1" Color="#007F7F7F" />
                </RadialGradientBrush>
            </Ellipse.OpacityMask>

            <Ellipse.Triggers>
                <EventTrigger RoutedEvent="Ellipse.Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                            <ColorAnimation
                                Storyboard.TargetProperty="(Ellipse.OpacityMask).(RadialGradientBrush.GradientStops)[0].Color"
                                From="#007F7F7F" To="#3F7F7F7F"
                                Duration="0:0:1.5" />
                            <ColorAnimation
                                Storyboard.TargetProperty="(Ellipse.OpacityMask).(RadialGradientBrush.GradientStops)[1].Color"
                                From="#007F7F7F" To="#807F7F7F"
                                Duration="0:0:1.5" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Ellipse.Triggers>
        </Ellipse>
    </Canvas>-->
</UserControl>